# script bash for steamOS

## swapFile
define swapfile size (8192 Mo, by default)

## coding
add pacman-key archlinux & version SteamOS (holo actualy, can not work for future update)
and install `base-devel glibc linux-api-headers` (require for gcc)

